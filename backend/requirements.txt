# FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic>=2.7.4    # Updated for langchain 0.3.0 compatibility
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Redis
redis==5.0.1
hiredis==2.3.2

# Celery
celery==5.3.6
flower==2.0.1
vine==5.1.0      # 固定版本，防止新版本移除 promise 接口导致 celery 启动失败
amqp==5.2.0      # celery 消息队列依赖，固定版本避免与 vine 不兼容

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
bcrypt==4.1.2

# Agent Libraries
openai==1.51.0                # OpenAI API 客户端
anthropic==0.18.1
langchain==0.2.17             # 稳定版本，避免 0.3.x 复杂依赖
langchain-core==0.2.43        # 与 langchain 0.2.17 兼容（最低 0.2.43）
langchain-openai==0.1.21      # OpenAI 集成，与 0.2.x 兼容
langchain-google-genai==1.0.10 # Google Gemini 接入（与 langchain 0.2.x 兼容）
langchain-community==0.2.18   # 社区集成
langchain-experimental==0.0.61 # 实验特性
google-generativeai==0.7.2    # Google Gemini 底层 SDK（与 langchain-google-genai 1.0.10 兼容）
pandas>=2.0.0                 # CSV 数据分析（CsvAgent 依赖）
duckduckgo-search>=6.0.0      # 免费网络搜索工具（无需 API Key）
pyyaml>=6.0                   # 读取 YAML 配置文件

# HTTP Client
httpx==0.26.0
aiohttp==3.9.3

# Utils
python-dotenv==1.0.1
email-validator==2.1.0
python-dateutil==2.8.2
pytz==2024.1

# Monitoring
prometheus-client==0.19.0

# Testing (dev)
pytest==7.4.4
pytest-asyncio==0.23.4
pytest-cov==4.1.0
httpx==0.26.0
